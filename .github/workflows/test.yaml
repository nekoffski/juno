name: Juno Test Pipeline

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Download dependencies
        run: go mod download
      - name: Install linters
        run: |
          go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.52.2
      - name: Run formatter
        run: make test-fmt
      - name: Run linter
        run: make lint
      - name: Run vet
        run: make vet

  build-and-unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Download dependencies
        run: go mod download
      - name: Install tools
        run: |
          go install github.com/deepmap/oapi-codegen/cmd/oapi-codegen@v1.16.3
          go install github.com/boumenot/gocover-cobertura@latest
      - name: Generate code
        run: make generate
      - name: Build binaries
        run: make build
      - name: Run unit tests with coverage
        run: make unit-test-cover
      - name: Upload unit coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: coverage.txt
          flags: unit
          token: ${{ secrets.CODECOV_TOKEN }}

  functional-tests:
    needs: build-and-unit-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Download dependencies
        run: go mod download
      - name: Install tools
        run: |
          go install github.com/deepmap/oapi-codegen/cmd/oapi-codegen@v1.16.3
          go install github.com/boumenot/gocover-cobertura@latest
      - name: Generate code
        run: make generate

  docker-build:
    needs: build-and-unit-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build lan agent container
        run: |
          docker build -f Dockerfile.lan .
      - name: Build core containers
        run: |
          docker compose --env-file ./conf/.env.example.docker build
