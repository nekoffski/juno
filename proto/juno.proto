syntax = "proto3";

package test;

message PingRequest {
    string uuid = 1;
}
message PongResponse {
    string uuid = 1;
}

service JunoService {
    rpc Ping (PingRequest) returns (PongResponse);
    rpc Test(PingRequest) returns (PongResponse);
}
