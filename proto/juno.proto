syntax = "proto3";

package juno.api;

message PingRequest {
    string magic = 1;
}
message PongResponse {
    string magic = 1;
}

service JunoService {
    rpc Ping (PingRequest) returns (PongResponse);
}
